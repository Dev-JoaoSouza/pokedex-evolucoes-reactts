export interface ICardEvolution {
    pokeId: number;
    pokeName: string;
    pokeImg: string;
    types: string[];
    save: boolean;
}

export interface ICardEvolutionStyled {
    pokeType: string;
}